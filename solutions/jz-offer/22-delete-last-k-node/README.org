删除一个节点，最简单直接的就是遍历两遍，第一遍计算链表的节点总数 n，
第二遍，要删除第 k 个节点，只要找到 n - k + 1 节点就可以了。

如果需要一次遍历（只把链表从头到尾遍历一次）完成，可以用双指针的形式（快慢指针），
快指针先遍历 k - 1 个节点，从第 k 步开始，慢指针从第一个节点开始，和快指针一起遍历，
直到快指针遍历到最后一个节点。此时，慢指针对应的节点，就是要删除的节点。
