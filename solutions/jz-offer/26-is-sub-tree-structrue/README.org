做这道题，需要掌握树的遍历。

思路是，遍历 A ，找到和 B 的根节点一样的节点 R，找到后判断 R 的子树和 B 的子树是否一致。

因此，第一步就是遍历 A ，找到和 B 的根节点值一样的节点 R。

第二步，遍历 R 的子树，和 B 对应的子树一一比较，看是否一致，则证明找到了。

如果不一致，则重复第一步，直到 A 遍历完。

另外，遍历树的时候，较简洁的实现是递归实现，用递归实现时，就要注意递归的结束条件，
以及遍历树节点时，可能会出现 null，要考虑好怎么处理。

实现前考虑好测试用例，测试边界条件，特殊输入等。
