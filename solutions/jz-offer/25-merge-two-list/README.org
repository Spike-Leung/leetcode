这题可以用四个指针完成，两个是题目给的，指向链表 1 (l1)和链表 2 (l2)的指针，
另外定义一个指针 l3，每次比较 l1 和 l2 的值，指向较小的一个。
定义一个 head 指针指向 l3 的头，当 l3 比较完，形成一个新的递增链表时，返回 head 即可。

需要考虑 l1，l2 可能为空，可能遍历一定次数后，l1 或 l2 为空，考虑有重复的值。

另外每次比较，都是比较 l1 和 l2 当前的值的大小，也可以使用递归去实现。
