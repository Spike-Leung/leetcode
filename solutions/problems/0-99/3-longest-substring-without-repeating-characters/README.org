这种在一个字符串中，查找连续的字串的问题，可以考虑滑动窗口，双指针去解决。

定义哈希表，存储遍历过的字符，用来判断是否重复。

然后定义双指针，left，right，不断地向右移动 right，直到出现重复字符，或者超过
字符串长度。此时，可以累计 left 和 right 之间的字符数量，为当前的最长字串长度。

如果出现重复字符了，则需要舍弃重复字符，从没重复的部分去算。哈希表中可以记录遍历
过的字符的下标，当重复时，left 移动到重复字符下标的下一位即可。
