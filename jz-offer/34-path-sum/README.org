从具体例子找规律，计算路径的时候，都是从根节点到叶子节点的，要从根节点一直遍历到叶子节点。
关于树的遍历，主要是前序遍历，中序遍历，后序遍历，只有前序遍历是从根节点开始的。

由于题目中的节点没有指向父节点的指针，那么为了保存路径，就需要一个数据结构将父节点的值存
起来。

然后考虑怎么遍历找到路径，从根节点往叶子节点遍历，每次遍历，累计和并且和目标值比较，同时
存储节点的值到数据结构中。当遍历到叶子节点时，如果和的大小跟目标值一样，则把当前路径存储
一下。如果和不一样，则需要返回上一层的父节点，继续遍历其子节点，此时，需要把节点从和中减去，
以及数据结构中移除。
